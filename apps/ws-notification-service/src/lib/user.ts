import { IUserConnection } from "@ws/types/ws";

export function getUserForSpecificNotification(
    message: string,
    connection: Map<string, IUserConnection>
) {
    // find user to send notification   
}